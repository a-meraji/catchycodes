import { Data } from "../utils";

const ABOUT = new Data("ABOUT");

const SERVICES = new Data("SERVICES");

const WORKS = new Data("WORKS");

const CONTACT = new Data("CONTACT");

export const allNavBarIcon = [SERVICES, WORKS, ABOUT, CONTACT];
