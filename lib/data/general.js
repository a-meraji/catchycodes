import { Data } from "../utils";

const ABOUT = new Data({title:"ABOUT", url:"#about"});

const SERVICES = new Data({title:"SERVICES", url:"#services"});

const WORKS = new Data({title:"WORKS", url:"/works"});

export const allNavBarIcon = [SERVICES, ABOUT, WORKS];
